Metadata-Version: 2.4
Name: docx-automation-service
Version: 0.1.0
Summary: DOCX format-preserving compliance automation service
Author: Thesis Project Team
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.115.0
Requires-Dist: uvicorn[standard]>=0.30.0
Requires-Dist: python-docx>=1.1.2
Requires-Dist: pydantic>=2.8.0
Requires-Dist: pydantic-settings>=2.4.0
Requires-Dist: python-multipart>=0.0.9
Requires-Dist: httpx>=0.27.0
Requires-Dist: textstat>=0.7.3
Provides-Extra: dev
Requires-Dist: pytest>=8.2.0; extra == "dev"
Requires-Dist: pytest-asyncio>=0.23.8; extra == "dev"

# DOCX Automation Service

面向论文场景的格式无损自动化服务，基于你文档中的四阶段策略实现：
1. 高保真摄入与语义分块
2. 双流检测（重复风险 + AIGC 风险）
3. 严格约束改写（保留引用与术语）
4. 格式感知回注（Run 级替换，尽量不破坏样式）

## 1. 安装

```bash
cd docx_automation_service
python -m pip install -e ".[dev]"
```

## 2. 配置

复制 `.env.example` 为 `.env`，填入你的硅基流动 API Key：

- `DOCX_AUTOMATION_SILICONFLOW_API_KEY`

## 3. 启动

```bash
uvicorn docx_automation_service.main:app --reload --port 8096
```

## 3.1 简易前端（可单用）

启动后直接访问：

- `http://127.0.0.1:8096/app`

该页面可完成：上传 DOCX、选择 analyze/rewrite、查看报告、下载改写结果。

## 3.2 主系统接入方式

你可以二选一：

- API 接入：主系统直接调用 `/v1/runs`、`/v1/runs/{run_id}/report`、`/v1/runs/{run_id}/result`
- 页面嵌入：主系统通过 iframe 嵌入 `http://127.0.0.1:8096/app`

如需跨域调用，请在 `.env` 设置：

- `DOCX_AUTOMATION_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173`

## 4. 外部接口（供论文生成应用调用）

### 创建处理任务

`POST /v1/runs`

- Content-Type: `multipart/form-data`
- 字段:
  - `file`: DOCX 文件
  - `mode`: `analyze` 或 `rewrite`
  - `topic_hint` (optional): 主题提示
  - `preserve_terms` (optional): 逗号分隔术语白名单

返回：
- `run_id`
- `status`
- `report_url`
- `result_url`（仅 rewrite 可下载）

### 查询报告

`GET /v1/runs/{run_id}/report`

返回每个分块的检测分数、是否命中阈值、是否改写。

### 下载结果文件

`GET /v1/runs/{run_id}/result`

下载改写后的 DOCX 文件。

### 健康检查

`GET /health`

## 5. 反 AIGC 检测四大策略

改写逻辑基于最新 AIGC 检测原理（困惑度 Perplexity + 突发性 Burstiness），采用四维语言重构防止"AI 文风"：

### 5.1 策略一：人设降维与语态主动化
- **理论**：AI 默认以"全知全能"的绝对客观视角输出，导致大量被动句（"被设计"、"被确立"）
- **做法**：强制第一人称或主动语态（"我们实现"而非"被实现"）；降低单句专业术语密度（≤3个）
- **效果**：打破机械客观感，接近真实工程师思维

### 5.2 策略二：结构突变与节奏控制
- **理论**：AI 生成文本句长极其均匀（15-25 词），结构四平八稳（低突发性）
- **做法**：强制长短句交替；复杂逻辑后必跟 7 字以内的极简短句（"这是关键。""系统必须拒绝。"）
- **效果**：拟态人类写作的不规律性和变化的字数分布

### 5.3 策略三：智力犹豫与工程细节注入
- **理论**：AI 逻辑过度完美，缺乏工程现实中的妥协、失败经验和具体细节
- **做法**：暴露工程妥协（"虽然理想是X，但受制于成本改用Y"）；硬插代码片段、JSON 格式、正则例子（不删除）
- **效果**：注入真实的复杂度和"脏活累活"感，破坏 AI 的纯概率预测链路

### 5.4 策略四：黑名单词汇系统性剔除
- **黑名单**（完全删除 & 替换）：
  - `总而言之` → `总结`
  - `值得注意的是` → `需要注意`
  - `不可避免地` → `必然` / `势必`
  - `由此产生的` → `导致`
  - `神经符号` → `符号`
  - `架构` → `系统结构` / `设计`
  - `代偿` → `弥补`
  - `坍塌` → `失效` / `崩溃`
  - `隔离层` → `拦截器`
  - `维度` → `方面`
  - `范式` → `方案`
  - `伦理约束` → `规则` / `限制`
  - `宏大叙事` / `排比对仗` → 改为技术文档风格

## 5.5 黑名单匹配与自动替换

Service 在改写阶段自动：
1. 调用 SiliconFlow 时强制上述四大策略的 system/user prompt
2. 重试超时自动降级到原文（不会整篇文档失败）
3. 在最终报告中标记哪些分块因超时/错误保留了原文

## 6. 对接建议

论文生成应用可在终稿导出后调用本服务：
1. 上传 DOCX 至 `POST /v1/runs`，`mode=rewrite`，可选 `topic_hint`（如"机器学习论文"）
2. 轮询 `GET /v1/runs/{run_id}/report` 查看分块检测结果
3. 完成后拉取 `GET /v1/runs/{run_id}/result` 获取改写 DOCX

建议在 `topic_hint` 中提供论文具体方向（如"深度学习"、"自然语言处理"），有助于改写时保留领域专有术语。

## 7. 说明与限制

- **检测器选型**：当前重复检测/AI 风险检测默认使用本地启发式实现，可替换为 Turnitin/Copyleaks/GPTZero 等商业适配器
- **超时与重试**：单个分块若 SiliconFlow 超时，默认重试 2 次（指数退避 1.5s 倍数），仍超时则回退原文并继续处理下一分块
- **回注策略**：格式优先，复杂跨 Run 场景采用"首 Run 注入 + 后续 Run 清空"方式保证样式稳定
- **报告与失败**：最终报告中 `rewrite_failures` 记录失败分块 ID 与错误原因，便于溯源
